#System-info

whoami – shows current user

uname -a – shows system and kernel info

history – shows previously used commands

sudo – runs command as superuser

sudo su - runs every command after this to run in root
