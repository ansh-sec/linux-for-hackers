#Networking Commands

ip a – shows network interfaces and IPs

ip route – shows routing table

ss -tuln – lists listening ports and services

ping – checks connectivity to a host

traceroute – shows packet path to a destination

nmap – scans ports and services on a target (we'll learn about it in later commits)
